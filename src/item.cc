#include "item.h"
#include <string>

using namespace std;

item::item(int item_id, string item_name, string item_description) :
   item_id(item_id), item_name(item_name), item_description(item_description) {}


